CREATE TABLE Status (
    Id INT(2) NOT NULL AUTO_INCREMENT,
    Name VARCHAR(30) NOT NULL,
    CreatedBy VARCHAR(10),
    ModifiedBy VARCHAR(10),
    CreatedDate DATE,
    ModifiedDate DATE,
    PRIMARY KEY (Id)
) ENGINE = INNODB;

INSERT INTO Status (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate) VALUES ("Inactive", "System", "System", Now(), Now());
INSERT INTO Status (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate) VALUES ("Active", "System", "System", Now(), Now());
INSERT INTO Status (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate) VALUES ("Submitted", "System", "System", Now(), Now());
INSERT INTO Status (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate) VALUES ("Approved", "System", "System", Now(), Now());
INSERT INTO Status (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate) VALUES ("Rejected", "System", "System", Now(), Now());
INSERT INTO Status (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate) VALUES ("Completed", "System", "System", Now(), Now());
INSERT INTO Status (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate) VALUES ("Expired", "System", "System", Now(), Now());

CREATE TABLE Role (
    Id INT(3) NOT NULL AUTO_INCREMENT,
    Name VARCHAR(30) NOT NULL,
    CreatedBy VARCHAR(10),
    ModifiedBy VARCHAR(10),
    CreatedDate DATE,
    ModifiedDate DATE,
    StatusId INT(2),
    PRIMARY KEY (Id),
    FOREIGN KEY (StatusId) REFERENCES Status(Id)
) ENGINE = INNODB;
INSERT INTO Role (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate, StatusId) VALUES ("Owner", "System", "System", Now(), Now(), 2);
INSERT INTO Role (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate, StatusId) VALUES ("Supervisor", "System", "System", Now(), Now(), 2);

CREATE TABLE User (
    Id INT(4) NOT NULL AUTO_INCREMENT,
    Name VARCHAR(30) NOT NULL,
    RoleId INT(3) NOT NULL,
    CreatedBy VARCHAR(10),
    ModifiedBy VARCHAR(10),
    CreatedDate DATE,
    ModifiedDate DATE,
    StatusId INT(2),
    PRIMARY KEY (Id),
    FOREIGN KEY (RoleId) REFERENCES Role(Id),
    FOREIGN KEY (StatusId) REFERENCES Status(Id)
) ENGINE = INNODB;

INSERT INTO User (Name, RoleId, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate, StatusId) VALUES ("user1", 1, "System", "System", Now(), Now(), 2);
INSERT INTO User (Name, RoleId, CreatedBy, ModifiedBy, CreatedDate,
ModifiedDate, StatusId) VALUES ("user2", 2, "System", "System", Now(), Now(), 2);

CREATE TABLE Task (
    Id BIGINT(20) NOT NULL AUTO_INCREMENT,
    Name VARCHAR(30) NOT NULL,
    CreatedBy INT(4) NOT NULL,
    ModifiedBy INT(4) NOT NULL,
    CreatedDate DATE,
    ModifiedDate DATE,
    StatusId INT(2) NOT NULL,
    PRIMARY KEY (Id),
    FOREIGN KEY (CreatedBy) REFERENCES User(Id),
    FOREIGN KEY (ModifiedBy) REFERENCES User(Id),
    FOREIGN KEY (StatusId) REFERENCES Status(Id)
) ENGINE = INNODB;

INSERT INTO Task (Name, CreatedBy, ModifiedBy, CreatedDate, ModifiedDate, StatusId) VALUES ("Task1", 1, 1, Now(), Now(), (select Id from Status where Name="Submitted"));
